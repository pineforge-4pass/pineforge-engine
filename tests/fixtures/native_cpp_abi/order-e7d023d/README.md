# Frozen e7d023d native_order_v1 closure

Exact `native_order_v1` header and `src/native_order.cpp` from
`e7d023dbdff1c98229155ec5bcdd1e4ac534f5fb` (tree
`0201bf052429490fb453bbfd6037e5afd1669626`). `CommandEvent` has 10
alternatives. The object is a compile/link control, not a historical runtime.
No executable is run.
